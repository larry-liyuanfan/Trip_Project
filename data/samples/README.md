# Sample Data

This folder contains a small mock OTA catalog used to validate the pipeline before integrating Yelp Open Dataset.

- `poi_catalog.jsonl`: POI candidates for retrieval.
- `reviews.jsonl`: review snippets for planning tests.
- `images/`: local image samples. Placeholder paths are used in Week 1 API examples.

