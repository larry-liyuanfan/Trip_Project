# Week 5 数据集标注与质检报告

## 当前结论

已实际构建并验证三个候选池，共 80,000 条、80,000 个唯一图片 SHA-256；与
Week 3 v1/v2 exclusion manifest 在 `source_id`、`image_sha256`、`group_id`
和 `constraint_template_id` 上均无冲突。通过全局 SSH 别名从 ECS 进程内临时读取
百炼密钥完成 3 条真实商品 smoke 预标注，3/3 Schema 合规、失败 0；密钥未写入
本地文件或日志。尚未收到 Week 5 人工标注与互审输入，因此人工修正、三级质检、
最终合格和多轮对话数量均为 0。目标数量未被写成完成数量。

2026-08-12 用户报告 A10 因欠费停机。当前本地可独立验证的 run B 恢复点为商品
15,166 条成功；远端最后一次监控约 36,615 条因服务器不可达而不能作为本报告的当前
文件实绩。已基于 15,166 条本地结果生成 Spartan migration：100 条 benchmark 和
64,734 条互斥分片，覆盖剩余 64,834 条。Spartan 作业尚未提交，因此这些数量是待
执行输入，不是新增预标注完成数。

### 2026-08-10 全量预标注实时文件复核

全量 run `week5_full_preannotation_qwen3_vl_4b_20260809_b` 的 `results.jsonl` 有
8,140 个唯一成功商品样本；`failures.jsonl` 中仍有 8 个未被后续成功记录覆盖的失败，
其中 Schema 错误 3、请求错误 5。当前商品全量进度为 8,140/50,000（16.28%），
总池进度为 8,140/80,000（10.175%）；售后和行程全量成功数均为 0。

checkpoint 最后更新时间为 2026-08-10 02:10:24（悉尼时间）。复核时本机没有运行中
的 Python/SSH 进程，`127.0.0.1:18001` 端口关闭，因此该 run 当前状态是
`partial / not running`，不是“仍在后台运行”。历史 pilot 不重复计入这组全量数量。

## 实际候选池

| 场景 | 目标 | 实际候选 | 预标注 | 人工修正 | 最终合格 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 以图搜商品 | 50,000 | 50,000 | 3 | 0 | 0 |
| 智能售后 | 20,000 | 20,000 | 0 | 0 | 0 |
| 多模态行程规划 | 10,000 | 10,000 | 0 | 0 | 0 |

上表的 3 条为早期独立 smoke。当前全量 run B 本地可验证预标注为商品 15,166、售后
0、行程 0；历史 smoke/pilot 与全量 run 不相加。三场景人工修正和最终合格仍均为 0。

### 商品分布

- 业态抽样层：酒店 200、景点 800、餐饮 49,000。评估来源组排除后仅剩 258
  张可用酒店图片，因此没有伪造三等分。
- 价位提示：budget 8,225、mid_range 32,824、premium 3,779、luxury
  426、unknown 4,746。
- 风格提示：casual 14,771、luxury 17,598、modern 3,224、romantic 218、
  unknown 14,189。
- 数量最多的城市：Philadelphia 6,710、New Orleans 5,141、Tampa 4,033、
  Nashville 3,697、Indianapolis 2,862、Tucson 1,965、Reno 1,910、
  Santa Barbara 1,863、Saint Louis 1,586、Edmonton 1,418。

以上均为来源元数据分层提示，不是人工图像金标。

### 售后分布

- 四类候选各 5,000：卫生污渍、设施损坏、景点关闭、交通延误。
- 来源：公开 Yelp 场景 5,552；Week 5 项目自有业务合成凭证 14,448。
- 严重度提示：low 3,610、medium 3,612、high 3,613、critical 3,613、
  unknown 5,552。公开场景不根据评论猜严重度，因此保持 unknown。

关键词和合成配方只用于路由，不是人工问题类型或严重度金标。公开弱配对仍需人工
查看图片，图像不支持问题时应修正或剔除。

### 行程分布

- 出行人群：solo/couple/family/friends 各 2,500。
- 预算：budget 3,336、mid_range 3,332、premium 3,332。
- 天数：2 天 3,336、3 天 3,336、4 天 3,328。

## 质检与对话实绩

| 项目 | 实际数量 |
| --- | ---: |
| 标注人员自审通过 | 0 |
| 同场景交叉互审通过 | 0 |
| 核心样本抽检通过 | 0 |
| 返工记录 | 0 |
| 剔除记录 | 0 |
| 商品最终合格 | 0 |
| 售后最终合格 | 0 |
| 行程最终合格 | 0 |
| 对话候选 | 0 |
| 对话人工校验 | 0 |
| 对话最终合格 | 0 |

原方案确定性待抽检集合为：商品 2,531/50,000（5.062%）、售后
2,032/20,000（10.16%）、行程 1,012/10,000（10.12%）。该历史口径已被
2026-08-10 单人最小人工方案替代，不再作为待执行集合，也不是已完成抽检数量。

2026-08-10 最新方案将人工修正保存与显式自审合并为一次真实操作。商品盲二次复核/
核心抽检为 0.2%/0.05%，售后和行程为 0.5%/0.1%；核心抽检使用相同哈希选择值并
嵌套于盲二次复核。同一真实操作者可以完成三阶段，但后续阶段必须使用不同
`review_session_id`。按现有 80,000 个候选的真实 `sample_id` 计算，盲复核/核心抽检
集合为商品 112/26、售后 102/21、行程 53/7，共 321 次额外阶段操作；这些是待处理
集合，不是实际完成数。上一版 1,677 次方案已被替代。

当前没有人工结果，不能计算或报告合格率和问题分布。多轮候选只能基于 Week 5
合格单轮样本生成；评估集样本不能拿来绕过这一条件。

## 已验证工程能力

- 三场景输出直接复用当前 accepted Schema：商品/售后 v1、行程 v2。
- Qwen3.7 Prompt 映射：商品与售后 `fewshot_4_v2`，行程
  `standardized_v4`；行程使用 2,560 token 配置。
- 预标注逐条追加，成功/失败均留痕，按 `sample_id` 跳过完成项，显式控制失败重试。
- 人工修正、revision、内联自审、确定性单人盲二次复核、嵌套核心抽检、返工和剔除
  均有可追溯记录；当前比例为商品 0.2%/0.05%、售后和行程 0.5%/0.1%。
- `export-quality` 自动排除未入固定集合、前序未通过或当前 revision 已处理的样本，
  避免唯一操作者手工筛选和重复审核。
- 对话结构强制 3–8 轮、角色交替、有效图片引用和五项人工检查。

验证结果：Week 5 定向测试 15/15 通过；完整 `unittest` 285/285 通过；样本池
校验返回 `status=ok`、80,000 个唯一 sample 和图片哈希；三个配置/Schema JSON
可解析；`git diff --check` 通过。

生成的候选、合成图片、哈希缓存、预标注、人工包、质检记录和对话均在
`outputs/week5_qwen3_vl_4b/`，保持 Git 忽略。

已实际导出三份本地人工包：商品 50,000、售后 20,000、行程 10,000。导出发生在
真实 smoke 前，包内 `model_preannotation_status=missing`；必须在全量预标注后重新
导出新文件，现有包不得被解释为可直接计入人工修正的完成包。

## 2026-08-09 Qwen3-VL-4B 受限行程 Prompt pilot

- 运行 ID：`week5_itinerary_prompt_pair_4b_20260809_a`；使用 30 条唯一 Week 5
  行程候选，对 `fewshot_4_v2` 与 `standardized_v4` 各请求一次，共 60 次，无重试。
- 审计完整性：60 个唯一请求哈希、30 个唯一输入哈希、60 份独立原始输出；
  `failures.jsonl` 保留 2 条 Schema 错误。候选 manifest SHA-256 为
  `4072260173f0b25cf7d5d63ab694f0849b351a483f42e4c39b9a99c5b9a17e75`。
- `fewshot_4_v2`：JSON 30/30、Schema 28/30、请求失败 0，平均 2,171.7 tokens，
  平均延迟 21,353.77 ms。
- `standardized_v4`：JSON 30/30、Schema 30/30、请求失败 0，平均 1,128.3 tokens，
  平均延迟 18,523.86 ms。
- 两个 Prompt 都没有可用的人工业务质量分，因此按已裁决的并列顺序先比较 Schema
  合规率，选择 `standardized_v4`；不以任何后续全量结果反向改选。
- 实测推理进程耗时 1,197.05 秒，按 CNY 18.3158/小时估算新增计算费 CNY 6.09；
  低于 60 请求、1 GPU 小时和 CNY 20 三个授权上限。

这 60 次请求只是模型 pilot。按胜出 Prompt 可得到 30 条行程预标注候选，但没有
真实人工修正、自审、交叉互审或核心抽检，因此三场景 `human_revised`、三级质检和
最终 `accepted` 数量仍全部为 0；多轮对话候选与人工合格数量也仍为 0。

已从胜出 Prompt 的 30 条有效结果导出版本化人工任务包
`outputs/week5_qwen3_vl_4b/human_tasks/itinerary_pilot_winner_20260809_a.jsonl`。
包内 annotator、人工标签、修订历史均为空，状态严格保持
`awaiting_human_annotation`；只有真实人工返回后才能导入下一状态。

## 2026-08-12 ECS 常驻迁移快照

- 候选池保持商品/售后/行程 50,000/20,000/10,000 不变；远端复核 80,000 个
  唯一图片引用，缺失 0，三份 manifest 哈希与活动 run 一致。
- 本地切换点为 checkpoint 15,190、成功结果 15,166；ECS systemd 恢复后的首次
  有效推进快照为 checkpoint 15,209、成功结果 15,197、连续请求失败 0。
- 迁移后活动 run ID、模型、Prompt、Schema 和 canonical config SHA-256 均未改变；
  本地 supervisor、runner 和 SSH 隧道已停止，服务器直接访问回环 vLLM。
- 该快照只表示模型预标注仍在运行。真实 `human_revised`、三级质检、最终
  `accepted` 和人工合格对话数量仍为 0。

## 2026-08-12 Spartan 接管快照

- migration ID：`week5_spartan_migration_20260812_a`。
- 本地可信基线：15,166；benchmark：100；分片：64,734；覆盖合计：80,000。
- benchmark 场景：商品 49、售后 36、行程 15。
- 四个分片记录数：16,093、16,106、16,169、16,366；分片间按 `sample_id` 确定性
  互斥，配置和 manifest 均记录 SHA-256。
- 当前 `queued/running/completed` GPU 作业数均为 0；原因是 Spartan project、quota、
  scratch 和登记账户提交尚未核验。
- 人工修正、自审、盲复核、核心抽检、最终 accepted、对话候选和对话 accepted 均仍
  为 0。
