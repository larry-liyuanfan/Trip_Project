# The CUDA-compatible vLLM base image already provides torch and transformers.
# These packages complete the Week 2 table, image, and progress-report runtime.
pandas>=2.2.0
pyarrow>=16.0.0
Pillow>=10.0.0
PyYAML>=6.0.0
tqdm>=4.66.0
