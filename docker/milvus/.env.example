# 复制为 docker/milvus/.env，并替换为仅供本机使用的新值。
MINIO_ROOT_USER=replace_with_local_user
MINIO_ROOT_PASSWORD=replace_with_local_password_at_least_8_chars

# 仅在 Milvus 开启鉴权时设置；默认 standalone 未开启鉴权。
MILVUS_TOKEN=
